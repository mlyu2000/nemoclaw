apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nemoclaw.fullname" . }}-openclaw-config
  labels:
    {{- include "nemoclaw.labels" . | nindent 4 }}
data:
  {{- if eq .Values.agent "openclaw" }}
  # ── OpenClaw config (openclaw.json) ────────────────────────────────────
  openclaw.json: |
    {
      "wizard": {
        "lastRunAt": "2026-09-03T00:00:00Z",
        "lastRunVersion": "2026.3.11",
        "lastRunCommand": "onboard",
        "lastRunMode": "local"
      },
      "agents": {
        "defaults": {
          "model": { "primary": "inference/{{ .Values.litellm.model }}" },
          "skipBootstrap": true,
          "thinkingDefault": "off",
          "workspace": "/sandbox/.openclaw/workspace"
        }
      },
      "models": {
        "mode": "merge",
        "providers": {
          "inference": {
            "baseUrl": {{ .Values.litellm.baseUrl | quote }},
            "apiKey": {{ .Values.litellm.apiKey | quote }},
            "api": "openai-completions",
            "models": [
              {
                "id": {{ .Values.litellm.model | quote }},
                "name": "inference/{{ .Values.litellm.model }}",
                "reasoning": false,
                "input": ["text"],
                "contextWindow": {{ .Values.litellm.contextWindow }},
                "maxTokens": {{ .Values.litellm.maxTokens }}
              }
            ]
          }
        }
      },
      "channels": {
        "defaults": {}{{- if .Values.telegram.enabled }}
        ,
        "telegram": {
          "enabled": true,
          {{- if .Values.telegram.allowAll }}
          "dmPolicy": "open",
          "groupPolicy": "open",
          "allowFrom": ["*"],
          "accounts": {
            "default": {
              "botToken": {{ .Values.telegram.botToken | quote }},
              "dmPolicy": "open",
              "groupPolicy": "open",
              "allowFrom": ["*"]
            }
          }
          {{- else }}
          "accounts": {
            "default": { "botToken": {{ .Values.telegram.botToken | quote }} }
          }
          {{- end }}
        }{{- end }}
      },
      "update": { "checkOnStart": false },
      "gateway": {
        "mode": "local",
        "bind": "{{ .Values.gateway.bind }}",
        "port": {{ .Values.gateway.port }},
        "controlUi": {
          "allowInsecureAuth": true,
          "dangerouslyDisableDeviceAuth": {{ .Values.gateway.dangerouslyDisableDeviceAuth }},
          "allowedOrigins": [{{ .Values.gateway.allowedOrigins | quote }}],
          "root": "{{ .Values.ui.root }}"
        },
        "auth": { "token": {{ .Values.gateway.token | quote }} }
      }
    }

  # Boot-time UI patcher: copies the control-ui to a writable dir and injects
  # a same-origin auto-token.js (CSP `script-src 'self'` blocks inline scripts)
  # that auto-connects the dashboard from the bare URL (the "Open" button URL).
  patch-ui.js: |-
{{ .Files.Get "files/patch-ui.js" | indent 4 }}

  {{- if .Values.telegram.enabled }}
  # One-off Telegram channel verification (runs once per pod start, BEFORE the
  # gateway). Reads TELEGRAM_BOT_TOKEN from the environment, runs getMe
  # (validity + egress), then sends a single test message to a chat id.
  telegram-verify.js: |-
{{ .Files.Get "files/telegram-verify.js" | indent 4 }}
  {{- end }}

  {{- else if eq .Values.agent "hermes" }}
  # ── Hermes Agent config (written to /sandbox/.hermes/ at pod start) ─────
  # seed-hermes-config.sh is sourced by the container entrypoint to write
  # ~/.hermes/config.yaml + .env before `hermes gateway run`.
  #
  # The real API_SERVER_KEY and Telegram bot token come from env vars (the
  # Secret). The config.env carries the placeholder structure so the seed
  # script produces valid config.yaml even without Secrets mounted.
  hermes-config.env: |
    HERMES_MODEL={{ .Values.hermes.model }}
    HERMES_LITELLM_BASE_URL={{ .Values.litellm.baseUrl }}
    HERMES_LITELLM_API_KEY={{ .Values.litellm.apiKey }}
    HERMES_LITELLM_MODEL={{ .Values.litellm.model }}
    HERMES_API_SERVER_KEY={{ .Values.hermes.apiServerKey }}
    TELEGRAM_BOT_TOKEN={{ .Values.telegram.botToken }}
    TELEGRAM_ALLOW_ALL={{ .Values.telegram.allowAll | default true }}
    {{- if .Values.telegram.enabled }}
    HERMES_TELEGRAM_ENABLED=true
    {{- end }}

  # Seed script: generates ~/.hermes/config.yaml + .env from hermes-config.env.
  seed-hermes-config.sh: |
    #!/usr/bin/env bash
    set -euo pipefail
    HERMES_HOME="${HERMES_HOME:-/sandbox/.hermes}"
    mkdir -p "$HERMES_HOME"
    CONFIG="$HERMES_HOME/config.yaml"
    ENV_FILE="$HERMES_HOME/.env"
    if [ -f /etc/openclaw-config/hermes-config.env ]; then
      set -a
      . /etc/openclaw-config/hermes-config.env
      set +a
    fi
    cat > "$ENV_FILE" <<ENVEOF
    API_SERVER_KEY=${HERMES_API_SERVER_KEY}
    HERMES_HOME=$HERMES_HOME
    API_SERVER_PORT=18642
    API_SERVER_HOST=127.0.0.1
    ENVEOF
    chmod 600 "$ENV_FILE"
    cat > "$CONFIG" <<CONFIGEOF
    config_version: 33
    model:
      default: ${HERMES_MODEL}
      provider: custom
      base_url: ${HERMES_LITELLM_BASE_URL}
    providers:
      custom:
        api_key: ${HERMES_LITELLM_API_KEY}
        base_url: ${HERMES_LITELLM_BASE_URL}
        api: openai-completions
        models:
          - id: ${HERMES_LITELLM_MODEL}
            name: ${HERMES_LITELLM_MODEL}
    custom_providers:
      - provider: custom
        base_url: ${HERMES_LITELLM_BASE_URL}
        api_key: ${HERMES_LITELLM_API_KEY}
        discover_models: true
    _nemoclaw_upstream:
      provider: custom
      model: ${HERMES_LITELLM_MODEL}
    api_server:
      enabled: true
      port: 18642
      host: 127.0.0.1
    platforms:
      api_server:
        enabled: true
      telegram:
        enabled: ${HERMES_TELEGRAM_ENABLED:-false}
    plugins:
      enabled: [nemoclaw]
    CONFIGEOF
    chmod 600 "$CONFIG"
    echo "[seed] Wrote $CONFIG and $ENV_FILE"
  {{- end }}
